%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%                                  %%%
%%%  DM Styles version 4.0  -  2017  %%%
%%%                                  %%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% NOTES ON COMPILING
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% if you want pdflatex features along with a small output file size, you can use the Ghostscript command:
% gs -dBATCH -dNOPAUSE -q -sDEVICE=pdfwrite -sOutputFile="Compressed.pdf" "Original.pdf"
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\ProvidesPackage{DM_Styles}

%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% Styles structure
% + Document Layout and style
% + Language and encoding
% + Symbols and fonts 
% + Document environment commands and renewcommands
% + Image and figure
% + Table
% + Math
% + Physics
% + Chemistry
% + Source code
% + References and Bibliography
% + Utility packages 
% + Custom commands
% + 


%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% DOCUMENT LAYOUT AND STYLE
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%
% Section numbering depth for Table of Contents %%
% Section numbering depth. Left as default = 2
\setcounter{secnumdepth}{2}
% The depth of table of contents. Left as default = 3
\setcounter{tocdepth}{3}

%%%%%
% Custom margins modified by package
\usepackage{geometry}
\geometry{
  letterpaper,
  left=3.5cm,
  right=2cm,
  top=2.5cm,
  bottom=2cm,
}

%%%%%
\usepackage{color}  % it adds support for colored text.
 
% \definecolor{dkgreen}{rgb}{0,0.6,0}
% \definecolor{gray}{rgb}{0.5,0.5,0.5}
% \definecolor{mauve}{rgb}{0.58,0,0.82}
%


%%%%%
\usepackage{fancyhdr} % to change header and footer of any page of the document.

%\newcommand{\changefont}{%
%      \fontsize{8}{9}\selectfont
%	}
%\pagestyle{fancy}
%% First you need to clear the default layout:
%\fancyhf{}
%\fancyfoot[CE,CO]{\thepage}
%\fancyhead[LE]{\changefont \slshape \leftmark}
%\fancyhead[RO]{\changefont \slshape \rightmark}
%
% habilitar estilo, opciones <<plain>>,<<fancy>> <<headings>>
%\pagestyle{fancy}
% First you need to clear the default layout:
%\fancyhf{}
%\fancyfoot[LE,RO]{\thepage}
%\fancyhead[CE]{\leftmark}
%\fancyhead[CO]{\rightmark}
% 
% There are seven letters you need to know before you can define your own header/footer:
% 
% E: Even page
% O: Odd page
% L: Left field
% C: Center field
% R: Right field
% H: Header
% F: Footer
% 
% Now it’s time to start defining your own layout. The definitions are added to the preamble:
% 
% 1	\fancyhead[CO,CE]{---Draft---}
% 2	\fancyfoot[C]{Confidential}
% 3	\fancyfoot[RO, LE] {\thepage}
% 
% The decorative lines can be changed by increasing/decreasing their thickness (0pt means no line):
% 
% 1	\renewcommand{\headrulewidth}{0.4pt}
% 2	\renewcommand{\footrulewidth}{0.4pt}


%%%%%
% Creates a page with layout definitions when \layout{} is invoked 
\usepackage{layout}

%%%%%
% To create the list of symbols used
\usepackage{nomencl}

%%%%%
% use a numbered list but as continuous paragraph
\usepackage{paralist}



%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% LANGUAGE AND ENCODING 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%
\usepackage[spanish,english]{babel}

%%%%%
\usepackage[T1]{fontenc}

%%%%%
\usepackage{hyphenat}

%%%%%
\usepackage[utf8]{inputenc}

%%%%%
% The hyperref package is used to handle cross-referencing commands in LaTeX to produce hypertext links in the document
\usepackage{hyperref}

%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% SYMBOLS AND FONTS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%
% Provides extra symbols, e.g. arrows like \textrightarrow, various currencies (\texteuro,...), things like \textcelsius and many other
\usepackage{textcomp}

%%%%%
%  Upright Greek letters. A package to provide the upright Greek letters from the Euler or Adobe Symbol fonts as additional math symbols
\usepackage{upgreek}

%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% DOCUMENT ENVIRONMENT COMMANDS

%%%%%
% Cite with author,date format with package natbib
\renewcommand{\cite}[1]{\citep{#1}}

%%%%%
% Name of the symbol section with package nomencl
\renewcommand{\nomname}{List of symbols and abreviations}

%%%%%
% the default font set to sans serif (easy readable)
%\renewcommand*{\familydefault}{\sfdefault}

%%%%%
% Rename abstract if needed
% \renewcommand{\abstractname}{executive summary}

%%%%%
% Definition environment 
\newtheorem{definition}{Definition}

%%%%%
% Remark environment
\newenvironment{remark}{\noindent\textbf{Remark}\newline\noindent}{\newline}
\newenvironment{remarks}{\noindent\textbf{Remarks}\begin{itemize}}{\end{itemize}}


%%%%%
%  Multiple columns
\usepackage{multicol}
% % Usage 
% 
% \begin{multicols}{ number of columns }
% 
% % these lines are optional and function to 
% % \lipsum[1-2] % generates filler text
% % \vfill  % Fills a vertical space
% % \columnbreak % breaks the column so starts the next column
% % 
% 
% \end{multicols}



%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% IMAGE AND FIGURE
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%
% Allows customization of appearance and placement of captions for figures, tables, etc.
\usepackage[font=small,labelfont=bf,margin=1.5cm]{caption} 
%[font=normalsize,labelfont=bf] 
%[font=small,labelfont=bf,margin=1.5cm] 


%%%%%
\usepackage{float} % this enables the ``H'' option to force figures to stay at the place where they where declared

%%%%%
\usepackage{graphicx}

%%%%%
% Overimpose pictures
\usepackage{overpic} 

%%%%%
% captions on side of the figure
\usepackage{sidecap}  
%  usage
% \begin{scfigure}
%   \centering
%   \includegraphics[width=0.5\textwidth]%
%     {giraff_picture}% picture filename
%   \caption{ ... caption text ... }
% \end{scfigure}


%%%%%
% varias figuras en un entorno figura 
\usepackage{subcaption}  
% usage: this example creates 3 images with her own captions in the same figure environment
% \begin{figure}
%         \begin{subfigure}[b]{0.3\textwidth}
%                 \centering
%                 \includegraphics[width=\textwidth]{gull}
%                 \caption{a gull}
%                 \label{fig:gull}
%         \end{subfigure}%
%         ~ %add desired spacing between images, e. g. ~, \quad, \qquad etc. 
%           %(or a blank line to force the subfigure onto a new line)
%         \begin{subfigure}[b]{0.3\textwidth}
%                 \centering
%                 \includegraphics[width=\textwidth]{tiger}
%                 \caption{a tiger}
%                 \label{fig:tiger}
%         \end{subfigure}
%         ~ %add desired spacing between images, e. g. ~, \quad, \qquad etc. 
%           %(or a blank line to force the subfigure onto a new line)
%         \begin{subfigure}[b]{0.3\textwidth}
%                 \centering
%                 \includegraphics[width=\textwidth]{mouse}
%                 \caption{a mouse}
%                 \label{fig:mouse}
%         \end{subfigure}
%         \caption{pictures of animals}\label{fig:animals}
% \end{figure}

%%%%%
% The text can lie around the figure, (text wraps the figure)
\usepackage{wrapfig} 
% usage
% \begin{wrapfigure}[lineheight]{alignment}{width}
%   \begin{center}
%     \includegraphics[width=0.48\textwidth]{image}
%   \end{center}
%   \caption{an image wrapped} 
% \end{wrapfigure}



%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% TABLE
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%
% Adding multiple rows in latex tables
% -----------------------------
% | this       | is | a table |
% -----------------------------
% | this spans | 1  | 3	   |
% -            ----------------
% | two rows   | 2  | 4	   |
% -----------------------------
% | one row    | 5  | 6	   |
% -----------------------------
\usepackage{multirow}

%%%%%
% Rotate tables to landscape mode in text %%
% This package requires the use of package `geometry`
\usepackage{pdflscape}
% An example of usage is
%\newgeometry{margin=1cm} % modify this if you need even more space
%\begin{landscape}

  %put your table here

%\end{landscape}
%\restoregeometry

%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% MATH
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%
% Adds more mathematical symbols
\usepackage{amssymb} 

%%%%%
\usepackage{array}

%%%%%
% Provides commands for striking out mathematical expressions
\usepackage{cancel}  
% the syntax is
% \cancel{x}
% 
% or
% 
% \cancelto{0}{x}

%%%%%
\usepackage{dsfont} 

%%%%%
% Adds additional integral symbols, for integrals over squares, clockwise integrals over sets, etc.
%\usepackage{esint}  

%%%%%
\usepackage{latexsym} 

%%%%%
% It contains the advanced math extensions for latex
%\usepackage{amsmath}
% mathtools includes amsmath
\usepackage{mathtools}

%%%%%
\usepackage{upgreek} 


%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% PHYSICS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% CHEMISTRY
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%
% chemical structures, figures and reactions
% To use it make chemical reaction arrows. Read the manual.
% A \chemrel [above][below]{-> or <- or <-> or <>, color, thicknes} B \par
% this creates A->B A<-B A<->B A <> B reaction schemes
%
\usepackage{chemfig}
% Configure chemical drawings style.
% comment the lines below to obtain the plain chemical molecules and stuff.
\setdoublesep{0.35700 em}  % 'Bond Spacing'
\setatomsep{1.78500 em}    % 'Fixed Length'
\setbondoffset{0.18265 em} % 'Margin Width'
\newcommand{\bondwidth}{0.06642 em} % 'Line Width'
\setbondstyle{line width = \bondwidth}


%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% SOURCE CODE
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{listings} % source code on the text
% 
% usage of listings and color

% \begin{lstlisting}
% put your code here
% \end{lstlisting}
% 
% another possibility,  is to import the code from the source itself. 
% this way, if you modify the source, you just have to recompile 
% the latex code and your document will be updated.
% 
% \lstinputlisting{source_filename.py}
% 
% in the example there is a python source, but it doesn't matter: 
% you can include any file but you have to write the full file name. 
% it will be considered plain text and it will be highlighted according to
% your settings, that means it doesn't recognize the programming language by itself.
% you can specify the language while including the file with the following command:
% 
% \lstinputlisting[language=python]{source_filename.py}

%  you can modify some style by doing the deffinitions

% \lstset{ %
%   language=octave,                % the language of the code
%   basicstyle=\footnotesize,           % the size of the fonts that are used for the code
%   numbers=left,                   % where to put the line-numbers
%   numberstyle=\tiny\color{gray},  % the style that is used for the line-numbers
%   stepnumber=2,                   % the step between two line-numbers. if it's 1, each line 
%                                   % will be numbered
%   numbersep=5pt,                  % how far the line-numbers are from the code
%   backgroundcolor=\color{white},      % choose the background color. you must add \usepackage{color}
%   showspaces=false,               % show spaces adding particular underscores
%   showstringspaces=false,         % underline spaces within strings
%   showtabs=false,                 % show tabs within strings adding particular underscores
%   frame=single,                   % adds a frame around the code
%   rulecolor=\color{black},        % if not set, the frame-color may be changed on line-breaks within not-black text (e.g. commens (green here))
%   tabsize=2,                      % sets default tabsize to 2 spaces
%   captionpos=b,                   % sets the caption-position to bottom
%   breaklines=true,                % sets automatic line breaking
%   breakatwhitespace=false,        % sets if automatic breaks should only happen at whitespace
%   title=\lstname,                   % show the filename of files included with \lstinputlisting;
%                                   % also try caption instead of title
%   keywordstyle=\color{blue},          % keyword style
%   commentstyle=\color{dkgreen},       % comment style
%   stringstyle=\color{mauve},         % string literal style
%   escapeinside={\%*}{*)},            % if you want to add latex within your code
%   morekeywords={*,...}               % if you want to add more keywords to the set
% }

%%%%%
% It defines the \url{...} command. 
\usepackage{url}

%%%%%
% It improves the verbatim environment, fixing some bugs. moreover, it provides the comment environment
\usepackage{verbatim} 


%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% REFERENCES AND BIBLIOGRAPHY
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%
% bibliography related, use natbib as bibtex engine
\usepackage{natbib}

%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% UTILITY PACKAGES 
% Fills text with Lorem Ipsum..
\usepackage{lipsum}


%% == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == == ==
% CUSTOM COMMANDS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%
% Import custom commands style sheet
\usepackage{DM_Custom_Commands}


